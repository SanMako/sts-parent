/**
 * 按钮类型
 */
export enum ButtonTypes {
  primary = "primary",
  ghost = "ghost",
  dashed = "dashed",
  link = "link",
  text = "text",
  default = "default",
  none = "",
}

/**
 * 按钮形状
 */
export enum ButtonShape {
  default = "default",
  ghost = "circle",
  dashed = "round",
  none = "default",
}

/**
 * 按钮大小
 */
export enum ButtonSize {
  large = "large",
  middle = "middle",
  small = "small",
  default = "middle",
}

/**
 * 国际化语言类型
 */
export enum LocaleType {
  default = "zh_CN",
  zh_CN = "zh_CN",
  en_US = "en_US",
}
